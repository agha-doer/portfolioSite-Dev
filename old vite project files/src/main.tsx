import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// File for the main entry point of the application
createRoot(document.getElementById("root")!).render(<App />);
